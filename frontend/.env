# Build-time defaults for PUBLIC_ variables only - never put secrets here.
# Real environments override these (actual env vars take precedence over .env).
# Empty means the waitlist section does not render.
PUBLIC_WAITLIST_URL=""
