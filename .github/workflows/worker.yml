# One workflow per component, triggered only when its files change (issue #13).
name: worker

on:
  pull_request:
    paths:
      - "worker/**"
      - ".github/workflows/worker.yml"
  push:
    branches: [main]
    paths:
      - "worker/**"
      - ".github/workflows/worker.yml"

permissions:
  contents: read

jobs:
  worker:
    # Self-hosted: docs/self-hosted-runner.md (system python3.11, not setup-python).
    runs-on: [self-hosted, Linux, X64, potocolom]
    defaults:
      run:
        working-directory: worker
    steps:
      - uses: actions/checkout@v7
      - run: python3.11 -m pip install -e ".[dev]"
      - run: python3.11 -m ruff check .
      - run: python3.11 -m mypy
      - run: python3.11 -m pytest
